query GetFolders($folder: ID, $onlyRoot: Boolean) {
    folders(onlyRoot: $onlyRoot, parent: $folder) {
        id
        name
        description
        color
        childrenQuantity
        gistsQuantity
    }
}